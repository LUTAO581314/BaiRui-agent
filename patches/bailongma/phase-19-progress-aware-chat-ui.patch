BACKUP=/home/hermes/backups/bailongma-phase19-20260608174448
--- /home/hermes/backups/bailongma-phase19-20260608174448/chat.js.before	2026-06-08 17:44:48.071124714 +0000
+++ src/ui/brain-ui/chat.js	2026-06-08 17:51:56.144640778 +0000
@@ -248,6 +248,70 @@
     closeTimer = setTimeout(closeChat, ms);
   }
 
+  const PROGRESS_LABELS = {
+    image_read: "\u6211\u5728\u770b\u56fe",
+    image_generate: "\u6211\u5728\u751f\u6210\u56fe\u7247",
+    search: "\u6211\u5728\u67e5\u8d44\u6599",
+    public_opinion: "\u6211\u5728\u770b\u70ed\u70b9",
+    company_task: "\u6211\u5728\u8bfb\u98de\u4e66\u4e0a\u4e0b\u6587",
+    memory_task: "\u6211\u5728\u6574\u7406\u8bb0\u5fc6",
+    high_risk: "\u7b49\u5f85\u4e3b\u4eba\u786e\u8ba4",
+    quick_question: "\u6211\u5728\u7ec4\u7ec7\u56de\u7b54",
+    casual_chat: "\u6211\u5728\u56de\u590d",
+  };
+
+  function ensureProgressEl() {
+    if (progressEl) return progressEl;
+    progressEl = document.createElement("div");
+    progressEl.className = "moxi-progress";
+    progressEl.setAttribute("role", "status");
+    progressEl.setAttribute("aria-live", "polite");
+    progressEl.hidden = true;
+    progressEl.innerHTML = '<span class="moxi-progress-dot"></span><span class="moxi-progress-text"></span><span class="moxi-progress-route"></span>';
+    chatHistory.insertBefore(progressEl, chatMessages);
+    return progressEl;
+  }
+
+  function progressText(data = {}) {
+    const route = String(data.route || "");
+    if (data.status === "ack_sent") return PROGRESS_LABELS[route] || "\u6211\u5728\u5904\u7406";
+    if (data.status === "worker_started") return PROGRESS_LABELS[route] || "\u6211\u5f00\u59cb\u5904\u7406\u4e86";
+    if (data.status === "worker_completed") return "\u5904\u7406\u5b8c\u6210\uff0c\u6b63\u5728\u6574\u7406\u56de\u590d";
+    if (data.status === "worker_failed") return "\u5904\u7406\u5361\u4f4f\u4e86\uff0c\u6b63\u5728\u6062\u590d";
+    return PROGRESS_LABELS[route] || "\u6211\u5728\u5904\u7406";
+  }
+
+  function updateProgress(data = {}) {
+    const el = ensureProgressEl();
+    if (progressHideTimer) {
+      clearTimeout(progressHideTimer);
+      progressHideTimer = null;
+    }
+    const status = String(data.status || "");
+    const route = String(data.route || "");
+    if (["worker_completed", "final_delivered", "failure_delivered", "cancelled"].includes(status)) {
+      clearProgress({ delayMs: status === "worker_completed" ? 1600 : 300 });
+      return;
+    }
+    el.hidden = false;
+    el.dataset.status = status || "active";
+    el.querySelector(".moxi-progress-text").textContent = progressText(data);
+    el.querySelector(".moxi-progress-route").textContent = route ? route.replace(/_/g, " ") : "";
+    openChat();
+  }
+
+  function clearProgress({ delayMs = 0 } = {}) {
+    if (!progressEl) return;
+    if (progressHideTimer) clearTimeout(progressHideTimer);
+    progressHideTimer = setTimeout(() => {
+      if (!progressEl) return;
+      progressEl.hidden = true;
+      progressEl.dataset.status = "";
+      progressEl.querySelector(".moxi-progress-text").textContent = "";
+      progressEl.querySelector(".moxi-progress-route").textContent = "";
+    }, Math.max(0, delayMs));
+  }
+
   function addMsg(role, text, options = {}) {
     const { alert = role === "jarvis", pending = true, label } = options;
     const defaultLabel = role === "user" ? "You" : role === "jarvis" ? getAgentName() : "Peer";
@@ -626,6 +690,8 @@
 
   return {
     addMsg,
+    updateProgress,
+    clearProgress,
     deleteLastUserMsg,
     updateLastJarvisMsg,
     beginLiveJarvisMsg,
--- /home/hermes/backups/bailongma-phase19-20260608174448/app.js.before	2026-06-08 17:44:48.075124758 +0000
+++ src/ui/brain-ui/app.js	2026-06-08 17:47:19.188915625 +0000
@@ -1611,6 +1611,8 @@
       break;
     case "message":
       if (data.from === "consciousness") {
+        if (data.moxi_progress) chat.updateProgress({ status: "ack_sent", route: data.route, job_id: data.job_id });
+        else chat.clearProgress({ delayMs: 300 });
         lastJarvisContent = data.content;
         const viaLabel = friendlyChannelLabel(data.channel);
         const content = viaLabel ? `_→ ${viaLabel}_  \n${data.content}` : data.content;
@@ -1663,6 +1665,9 @@
     case "person_card_mode":
       setPersonCardMode(!!data.active || data.action === "show" || data.action === "open" || data.action === "update", { source: "agent_event", card: data.card || null });
       break;
+    case "moxi_progress":
+      chat.updateProgress(data);
+      break;
     case "social_status":
       window.dispatchEvent(new CustomEvent("bailongma:social_status", { detail: data }));
       break;
--- /home/hermes/backups/bailongma-phase19-20260608174448/styles.css.before	2026-06-08 17:44:48.075124758 +0000
+++ src/ui/brain-ui/styles.css	2026-06-08 17:47:19.188915625 +0000
@@ -883,6 +883,53 @@
 #chat-messages::-webkit-scrollbar { width: 6px; }
 #chat-messages::-webkit-scrollbar-thumb { background: var(--line-strong); border-radius: 999px; }
 
+.moxi-progress {
+  display: flex;
+  align-items: center;
+  gap: 8px;
+  margin: 12px 18px 0;
+  padding: 8px 10px;
+  border: 1px solid color-mix(in srgb, var(--cool) 30%, transparent);
+  border-radius: 10px;
+  color: var(--cool);
+  background: color-mix(in srgb, var(--console-bg) 82%, var(--cool) 8%);
+  font-size: 12px;
+  line-height: 1.35;
+}
+
+.moxi-progress[hidden] { display: none; }
+
+.moxi-progress-dot {
+  width: 7px;
+  height: 7px;
+  border-radius: 999px;
+  background: var(--cool);
+  box-shadow: 0 0 0 4px color-mix(in srgb, var(--cool) 18%, transparent);
+  animation: moxi-progress-pulse 1.15s ease-in-out infinite;
+  flex: 0 0 auto;
+}
+
+.moxi-progress-text {
+  min-width: 0;
+  font-weight: 650;
+  overflow-wrap: anywhere;
+}
+
+.moxi-progress-route {
+  margin-left: auto;
+  padding: 2px 7px;
+  border-radius: 999px;
+  color: var(--ink2);
+  background: color-mix(in srgb, var(--line) 50%, transparent);
+  font-size: 11px;
+  white-space: nowrap;
+}
+
+@keyframes moxi-progress-pulse {
+  0%, 100% { transform: scale(0.82); opacity: 0.55; }
+  50% { transform: scale(1); opacity: 1; }
+}
+
 .msg {
   max-width: 85%;
   padding: 8px 12px;
