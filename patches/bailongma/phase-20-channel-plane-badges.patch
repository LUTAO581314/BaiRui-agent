BACKUP=/home/hermes/backups/bailongma-phase20-20260608175836
--- /home/hermes/backups/bailongma-phase20-20260608175836/chat.js.before	2026-06-08 17:58:36.298625792 +0000
+++ src/ui/brain-ui/chat.js	2026-06-08 17:58:36.338626413 +0000
@@ -260,6 +260,41 @@
     casual_chat: "\u6211\u5728\u56de\u590d",
   };
 
+  const PLANE_LABELS = {
+    personal: "\u4e2a\u4eba\u966a\u4f34",
+    company: "\u516c\u53f8\u7ba1\u7406",
+    approval: "\u4e3b\u4eba\u786e\u8ba4",
+    web: "\u7f51\u9875\u966a\u4f34",
+    runtime: "Runtime",
+  };
+
+  function planeForChannel(channel = "") {
+    const text = String(channel || "").toLowerCase();
+    if (text.includes("feishu") || text.includes("lark") || text.includes("wecom")) return "company";
+    if (text.includes("web")) return "web";
+    if (text.includes("wechat") || text.includes("qq")) return "personal";
+    return "web";
+  }
+
+  function planeForRoute(route = "", fallbackPlane = "") {
+    if (route === "high_risk") return "approval";
+    if (route === "company_task") return "company";
+    return fallbackPlane || "web";
+  }
+
+  function decorateMessage(div, { channel = "", route = "", plane = "", badge = "" } = {}) {
+    const finalPlane = planeForRoute(String(route || ""), plane || planeForChannel(channel));
+    const label = badge || PLANE_LABELS[finalPlane] || "";
+    if (!label) return;
+    div.dataset.plane = finalPlane;
+    if (route) div.dataset.route = String(route);
+    const badgeEl = document.createElement("span");
+    badgeEl.className = `msg-plane-badge msg-plane-${finalPlane}`;
+    badgeEl.textContent = label;
+    const labelSpan = div.querySelector(".msg-label");
+    if (labelSpan) labelSpan.after(badgeEl);
+  }
+
   function ensureProgressEl() {
     if (progressEl) return progressEl;
     progressEl = document.createElement("div");
@@ -313,7 +348,7 @@
   }
 
   function addMsg(role, text, options = {}) {
-    const { alert = role === "jarvis", pending = true, label } = options;
+    const { alert = role === "jarvis", pending = true, label, channel = "", route = "", plane = "", badge = "" } = options;
     const defaultLabel = role === "user" ? "You" : role === "jarvis" ? getAgentName() : "Peer";
     const labelText = label || defaultLabel;
     const div = document.createElement("div");
@@ -322,6 +357,7 @@
     labelSpan.className = "msg-label";
     labelSpan.textContent = labelText;
     div.appendChild(labelSpan);
+    decorateMessage(div, { channel, route, plane, badge });
     div.appendChild(createMarkdownBody(text));
     chatMessages.appendChild(div);
 
@@ -363,7 +399,7 @@
     const visibleText = images.length
       ? `${text || "请识别这张图片"}\n\n[已附加 ${images.length} 张图片]`
       : text;
-    addMsg("user", visibleText, { label: label || undefined });
+    addMsg("user", visibleText, { label: label || undefined, channel: channel || "WEB" });
     openChat();
     scheduleClose(1000);
     if (override === false) return;
@@ -690,6 +726,8 @@
 
   return {
     addMsg,
+    planeForChannel,
+    planeForRoute,
     updateProgress,
     clearProgress,
     deleteLastUserMsg,
--- /home/hermes/backups/bailongma-phase20-20260608175836/app.js.before	2026-06-08 17:58:36.302625854 +0000
+++ src/ui/brain-ui/app.js	2026-06-08 17:58:36.338626413 +0000
@@ -1611,7 +1611,7 @@
       break;
     case "message":
       if (data.from === "consciousness") {
-        if (data.moxi_progress) chat.updateProgress({ status: "ack_sent", route: data.route, job_id: data.job_id });
+        if (data.moxi_progress) chat.updateProgress({ status: "ack_sent", route: data.route, job_id: data.job_id, channel: data.channel });
         else chat.clearProgress({ delayMs: 300 });
         lastJarvisContent = data.content;
         const viaLabel = friendlyChannelLabel(data.channel);
@@ -1620,7 +1620,7 @@
         if (chat.hasLiveJarvisMsg()) {
           chat.finalizeLiveJarvisMsg(content);
         } else {
-          addMsg("jarvis", content);
+          addMsg("jarvis", content, { channel: data.channel, route: data.route, plane: data.moxi_progress ? "runtime" : "" });
         }
         // 语音轮的 TTS 收尾：逐句会话进行中 → flush 尾句并收尾；若未走逐句（流式合成关闭）→ 整段播一次
         if (liveTurnSpeak || isVoiceReplyEvent(data)) {
@@ -1642,7 +1642,7 @@
         || (data.from_id && /^(wechat|discord|feishu|wecom):/i.test(data.from_id));
       if (isExternal) {
         const label = friendlyChannelLabel(data.channel) || data.from_id || "External";
-        addMsg("external", data.content, { label, alert: false });
+        addMsg("external", data.content, { label, alert: false, channel: data.channel });
         openChat(true);
       }
       break;
--- /home/hermes/backups/bailongma-phase20-20260608175836/styles.css.before	2026-06-08 17:58:36.306625916 +0000
+++ src/ui/brain-ui/styles.css	2026-06-08 17:58:36.338626413 +0000
@@ -974,6 +974,42 @@
   max-width: 78%;
 }
 
+.msg-plane-badge {
+  display: inline-flex;
+  align-items: center;
+  max-width: 120px;
+  margin-left: 7px;
+  padding: 1px 6px;
+  border-radius: 999px;
+  border: 1px solid var(--line);
+  color: var(--ink2);
+  background: color-mix(in srgb, var(--console-bg) 80%, var(--line) 20%);
+  font-size: 10px;
+  line-height: 1.4;
+  white-space: nowrap;
+  vertical-align: middle;
+}
+
+.msg-plane-personal,
+.msg-plane-web {
+  border-color: color-mix(in srgb, var(--cool) 28%, transparent);
+  color: var(--cool);
+}
+
+.msg-plane-company {
+  border-color: color-mix(in srgb, var(--warm) 30%, transparent);
+  color: var(--warm);
+}
+
+.msg-plane-approval {
+  border-color: color-mix(in srgb, #ff5c7a 38%, transparent);
+  color: #ff8aa0;
+}
+
+.msg-plane-runtime {
+  border-color: color-mix(in srgb, var(--line-strong) 70%, transparent);
+}
+
 .msg-label {
   display: block;
   margin-bottom: 3px;
