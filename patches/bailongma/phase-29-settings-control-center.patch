--- a/src/api.js	2026-06-08 18:32:44.469661866 +0000
+++ b/src/api.js	2026-06-09 02:35:02.909785824 +0000
@@ -254,6 +254,11 @@
   return result.ok ? { connected: true, payload: result.payload } : { connected: false, reason: result.reason }
 }
 
+async function fetchHermesReadOnly(path) {
+  const result = await fetchHermesRuntimeJson(path)
+  return result.ok ? { connected: true, payload: result.payload } : { connected: false, reason: result.reason }
+}
+
 async function buildCapabilityMatrix() {
   const activation = getActivationStatus()
   const social = getSocialConfig()
@@ -1460,6 +1465,29 @@
       return
     }
 
+    // GET /performance ? proxy Hermes safe performance profile through BaiLongma
+    if (req.method === 'GET' && url.pathname === '/performance') {
+      const performance = await fetchHermesReadOnly('/performance')
+      if (!performance.connected) {
+        jsonResponse(res, 503, { ok: false, error: 'hermes_performance_unavailable', reason: performance.reason || 'unknown' })
+        return
+      }
+      jsonResponse(res, 200, { ok: true, source: 'bailongma-hermes-bridge', ...(performance.payload || {}) })
+      return
+    }
+
+    // GET /jobs ? proxy Hermes safe async-job list through BaiLongma
+    if (req.method === 'GET' && url.pathname === '/jobs') {
+      const limit = Math.max(1, Math.min(50, Number(url.searchParams.get('limit') || 10)))
+      const jobs = await fetchHermesReadOnly(`/jobs?limit=${limit}`)
+      if (!jobs.connected) {
+        jsonResponse(res, 503, { ok: false, error: 'hermes_jobs_unavailable', reason: jobs.reason || 'unknown' })
+        return
+      }
+      jsonResponse(res, 200, { ok: true, source: 'bailongma-hermes-bridge', ...(jobs.payload || {}) })
+      return
+    }
+
     // GET /settings — return current LLM + MiniMax configuration status
     if (req.method === 'GET' && url.pathname === '/settings') {
       const status = getActivationStatus()
--- a/src/ui/brain-ui/app-shell.js	2026-06-09 01:36:58.068311769 +0000
+++ b/src/ui/brain-ui/app-shell.js	2026-06-09 02:41:39.214306754 +0000
@@ -184,13 +184,16 @@
 
       <!-- 侧栏导航 -->
       <nav class="settings-nav">
-        <button class="settings-nav-item active" data-tab="appearance" type="button">外观</button>
-        <button class="settings-nav-item" data-tab="llm" type="button">LLM 模型</button>
-        <button class="settings-nav-item" data-tab="media" type="button">媒体能力</button>
-        <button class="settings-nav-item" data-tab="social" type="button">社交媒体</button>
-        <button class="settings-nav-item" data-tab="voice" type="button">语音识别</button>
-        <button class="settings-nav-item" data-tab="web-search" type="button">上网搜索</button>
-        <button class="settings-nav-item" data-tab="security" type="button">安全沙箱</button>
+        <button class="settings-nav-item active" data-tab="overview" type="button">总览</button>
+        <button class="settings-nav-item" data-tab="llm" type="button">模型</button>
+        <button class="settings-nav-item" data-tab="media" type="button">媒体</button>
+        <button class="settings-nav-item" data-tab="voice" type="button">语音</button>
+        <button class="settings-nav-item" data-tab="web-search" type="button">搜索</button>
+        <button class="settings-nav-item" data-tab="social" type="button">社交通道</button>
+        <button class="settings-nav-item" data-tab="memory" type="button">记忆</button>
+        <button class="settings-nav-item" data-tab="jobs" type="button">工具任务</button>
+        <button class="settings-nav-item" data-tab="security" type="button">安全</button>
+        <button class="settings-nav-item" data-tab="appearance" type="button">外观</button>
         <button class="settings-nav-item" data-tab="update" type="button">更新</button>
       </nav>
 
@@ -198,7 +201,31 @@
       <div class="settings-content">
 
         <!-- ── 外观 tab ── -->
-        <div class="settings-tab active" data-tab="appearance">
+        <div class="settings-tab active" data-tab="overview">
+          <div class="settings-hero">
+            <div>
+              <div class="settings-kicker">HERMES CONTROL PLANE</div>
+              <h3>MOXI 设置中心</h3>
+              <p>这里映射 Hermes 运行态、BaiLongma 前端适配、模型、媒体、语音、搜索、社交、记忆和工具任务。能直接保存的配置会提供输入框；只读运行态会明确标注。</p>
+            </div>
+            <div class="settings-hero-status" id="settings-overview-status">读取中</div>
+          </div>
+          <div class="settings-section settings-capability-panel">
+            <div class="settings-section-label">能力矩阵</div>
+            <div class="settings-capability-summary" id="capability-summary">正在读取能力状态...</div>
+            <div class="settings-capability-grid" id="capability-grid"></div>
+          </div>
+          <div class="settings-section">
+            <div class="settings-section-label">运行契约</div>
+            <div class="settings-matrix" id="settings-contract-grid"><div class="settings-empty">等待 Hermes /frontend/contract</div></div>
+          </div>
+          <div class="settings-section">
+            <div class="settings-section-label">性能预算</div>
+            <div class="settings-matrix" id="settings-performance-grid"><div class="settings-empty">等待 Hermes /performance</div></div>
+          </div>
+        </div>
+
+        <div class="settings-tab" data-tab="appearance">
           <div class="settings-section">
             <div class="settings-section-label">主题</div>
             ${createThemeSwitcher()}
@@ -300,10 +327,9 @@
 
         <!-- ── 社交媒体 tab ── -->
         <div class="settings-tab" data-tab="social">
-          <div class="settings-section settings-capability-panel">
-            <div class="settings-section-label">能力总览</div>
-            <div class="settings-capability-summary" id="capability-summary">正在读取能力状态...</div>
-            <div class="settings-capability-grid" id="capability-grid"></div>
+          <div class="settings-section settings-channel-intro">
+            <div class="settings-section-label">渠道配置</div>
+            <p class="settings-hint">这里只管理 Discord、飞书、微信公众号、企业微信、QQ 和个人微信 ClawBot 的连接信息。全局能力状态请在「总览」查看。</p>
           </div>
           <div class="settings-section">
             <div class="settings-section-label">Discord</div>
@@ -699,6 +725,34 @@
         </div>
 
         <!-- ── 安全沙箱 tab ── -->
+        <div class="settings-tab" data-tab="memory">
+          <div class="settings-section">
+            <div class="settings-section-label">记忆治理</div>
+            <p class="settings-hint">BaiLongma 运行记忆只作为工作记忆和候选图谱；长期记忆以 Obsidian 审核写回为准。这里先展示只读状态，避免把测试垃圾直接写成永久记忆。</p>
+            <div class="settings-matrix" id="settings-memory-grid"><div class="settings-empty">等待 /status 与 /memory/graph</div></div>
+          </div>
+          <div class="settings-section">
+            <div class="settings-section-label">梦境整理</div>
+            <p class="settings-hint">当记忆混乱、重复、过期或包含测试内容时，先生成 dream report，再由主人确认忘记、合并、归档或提升到 Obsidian。</p>
+            <div class="settings-config-row"><span class="settings-config-type">Source</span><span class="settings-config-info">BaiLongma /memory/graph -> Hermes memory_dream -> Obsidian review</span></div>
+            <div class="settings-config-row"><span class="settings-config-type">Write</span><span class="settings-config-info">默认只读；不自动删除、不自动长期保存</span></div>
+          </div>
+        </div>
+
+        <div class="settings-tab" data-tab="jobs">
+          <div class="settings-section">
+            <div class="settings-section-label">异步任务</div>
+            <p class="settings-hint">图片、搜索、舆情、公司查询和长推理应进入 Hermes job 流程，前端只显示进度和结果，不阻塞聊天。</p>
+            <div class="settings-matrix" id="settings-jobs-grid"><div class="settings-empty">等待 Hermes /jobs</div></div>
+          </div>
+          <div class="settings-section">
+            <div class="settings-section-label">工具调用边界</div>
+            <div class="settings-config-row"><span class="settings-config-type">Fast ACK</span><span class="settings-config-info">社交通道先给自然回应，再跑慢任务</span></div>
+            <div class="settings-config-row"><span class="settings-config-type">Approval</span><span class="settings-config-info">公司写入、交易、删除、账号配置必须主人确认</span></div>
+            <div class="settings-config-row"><span class="settings-config-type">Events</span><span class="settings-config-info">worker_started / worker_completed / final_delivered</span></div>
+          </div>
+        </div>
+
         <div class="settings-tab" data-tab="security">
           <div class="settings-section">
             <div class="settings-section-label">文件沙箱</div>
--- a/src/ui/brain-ui/app.js	2026-06-09 01:36:58.072311853 +0000
+++ b/src/ui/brain-ui/app.js	2026-06-09 02:46:05.128036242 +0000
@@ -2594,8 +2594,8 @@
 
   let cachedProviders = null;
 
-  function activateSettingsTab(tab = "appearance", { refresh = true } = {}) {
-    const selectedTab = tab || "appearance";
+  function activateSettingsTab(tab = "overview", { refresh = true } = {}) {
+    const selectedTab = tab || "overview";
     overlay.querySelectorAll(".settings-nav-item").forEach(btn => {
       btn.classList.toggle("active", btn.dataset.tab === selectedTab);
     });
@@ -2604,6 +2604,9 @@
     });
     if (!refresh) return;
     switch (selectedTab) {
+      case "overview":
+        loadHermesOverview();
+        break;
       case "llm":
       case "media":
         loadSettings();
@@ -2617,6 +2620,12 @@
       case "web-search":
         loadWebSearchSettings();
         break;
+      case "memory":
+        loadMemorySettings();
+        break;
+      case "jobs":
+        loadJobSettings();
+        break;
       case "security":
         loadSecuritySettings();
         break;
@@ -2778,9 +2787,79 @@
     }
   }
 
+
+  function renderSettingsPairs(containerId, pairs) {
+    const el = document.getElementById(containerId);
+    if (!el) return;
+    el.innerHTML = pairs.map(([label, value, tone = ""]) => `
+      <div class="settings-mini-card ${tone}">
+        <span>${label}</span>
+        <b>${value}</b>
+      </div>`).join("") || `<div class="settings-empty">暂无数据</div>`;
+  }
+
+  async function loadHermesOverview() {
+    loadCapabilityMatrix();
+    try {
+      const [contract, performance] = await Promise.all([
+        fetch(`${API}/frontend/contract`).then(r => r.json()).catch(() => null),
+        fetch(`${API}/performance`).then(r => r.json()).catch(() => null),
+      ]);
+      const statusEl = document.getElementById("settings-overview-status");
+      if (statusEl) statusEl.textContent = contract?.status || "READY";
+      const endpoints = contract?.endpoints || {};
+      renderSettingsPairs("settings-contract-grid", Object.entries(endpoints).map(([key, item]) => [key.replaceAll("_", " "), item?.path || "configured", "ready"]));
+      const profile = performance?.profile || performance?.performance || performance || {};
+      renderSettingsPairs("settings-performance-grid", [
+        ["quick ack", `${profile.quick_ack_delay_ms ?? "-"} ms`, "ready"],
+        ["fast reply", `${profile.fast_reply_target_ms ?? "-"} ms`, "ready"],
+        ["slow task", `${profile.slow_task_threshold_ms ?? "-"} ms`, "partial"],
+        ["async timeout", `${profile.async_task_timeout_seconds ?? "-"} s`, "partial"],
+      ]);
+    } catch {
+      const statusEl = document.getElementById("settings-overview-status");
+      if (statusEl) statusEl.textContent = "DEGRADED";
+    }
+  }
+
+  async function loadMemorySettings() {
+    try {
+      const [status, graph] = await Promise.all([
+        fetch(`${API}/status`).then(r => r.json()).catch(() => null),
+        fetch(`${API}/memory/graph?limit=80`).then(r => r.json()).catch(() => null),
+      ]);
+      const nodes = graph?.nodes || graph?.memory_nodes || [];
+      const edges = graph?.edges || graph?.links || [];
+      renderSettingsPairs("settings-memory-grid", [
+        ["runtime", status?.running === false ? "stopped" : "running", status?.running === false ? "missing" : "ready"],
+        ["memories", status?.memoryCount ?? status?.memories ?? nodes.length ?? "-", "ready"],
+        ["graph nodes", nodes.length ?? 0, "partial"],
+        ["graph links", edges.length ?? 0, "partial"],
+        ["durable source", "Obsidian review", "ready"],
+      ]);
+    } catch {
+      renderSettingsPairs("settings-memory-grid", [["memory", "unavailable", "missing"]]);
+    }
+  }
+
+  async function loadJobSettings() {
+    try {
+      const payload = await fetch(`${API}/jobs?limit=6`).then(r => r.json());
+      const jobs = payload?.jobs || [];
+      const rows = [
+        ["active jobs", jobs.filter(j => !["completed", "failed", "delivered", "cancelled"].includes(j.status)).length, "partial"],
+        ["recent jobs", jobs.length, "ready"],
+        ["allowed events", (payload?.allowed_events || []).length || "ready", "ready"],
+      ];
+      jobs.slice(-3).reverse().forEach(job => rows.push([job.kind || job.job_id || "job", job.status || "queued", ""]));
+      renderSettingsPairs("settings-jobs-grid", rows);
+    } catch {
+      renderSettingsPairs("settings-jobs-grid", [["jobs", "unavailable", "missing"]]);
+    }
+  }
+
   async function loadSocialSettings() {
     try {
-      loadCapabilityMatrix();
       const { social } = await fetch(`${API}/settings/social`).then(r => r.json());
       for (const [statusId, keys] of Object.entries(SOCIAL_PLATFORM_STATUS)) {
         const el = document.getElementById(statusId);
@@ -3168,7 +3247,7 @@
     overlay.hidden = false;
     const currentTab = tab
       || overlay.querySelector(".settings-nav-item.active")?.dataset.tab
-      || "appearance";
+      || "overview";
     activateSettingsTab(currentTab);
   }
 
--- a/src/ui/brain-ui/styles.css	2026-06-08 17:58:36.338626413 +0000
+++ b/src/ui/brain-ui/styles.css	2026-06-09 02:39:42.271795401 +0000
@@ -167,7 +167,7 @@
 .brand-mark {
   width: 30px;
   height: 30px;
-  border-radius: 8px;
+  border-radius: 16px;
   display: grid;
   place-items: center;
   flex-shrink: 0;
@@ -1609,11 +1609,11 @@
 .settings-overlay[hidden] { display: none; }
 
 .settings-modal {
-  width: 600px;
-  height: 560px;
+  width: 760px;
+  height: 660px;
   max-width: calc(100vw - 40px);
-  max-height: calc(100vh - 80px);
-  border-radius: 16px;
+  max-height: calc(100vh - 48px);
+  border-radius: 8px;
   border: 1px solid var(--line-strong);
   background: var(--bg1);
   backdrop-filter: blur(24px);
@@ -1819,7 +1819,7 @@
 }
 
 .settings-nav {
-  width: 112px;
+  width: 132px;
   flex-shrink: 0;
   border-right: 1px solid var(--line);
   padding: 10px 0;
@@ -4416,3 +4416,20 @@
   margin-bottom: 6px;
   font-family: var(--mono);
 }
+
+
+.settings-hero { display: flex; align-items: flex-start; justify-content: space-between; gap: 18px; padding: 18px; border-bottom: 1px solid var(--line); background: rgba(143,182,216,0.055); }
+.settings-kicker { font-size: 10px; font-family: "JetBrains Mono", ui-monospace, monospace; letter-spacing: 0.14em; color: var(--warm); margin-bottom: 8px; }
+.settings-hero h3 { margin: 0 0 8px; font-size: 16px; color: var(--ink); }
+.settings-hero p { margin: 0; max-width: 520px; font-size: 12px; line-height: 1.65; color: var(--ink2); }
+.settings-hero-status { flex: none; padding: 5px 8px; border: 1px solid var(--cool); border-radius: 4px; color: var(--cool); font-size: 10px; font-family: "JetBrains Mono", ui-monospace, monospace; letter-spacing: 0.1em; }
+.settings-matrix { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; }
+.settings-mini-card { min-height: 52px; padding: 10px 12px; border: 1px solid var(--line-strong); border-radius: 6px; background: rgba(255,255,255,0.025); }
+.settings-mini-card span { display: block; margin-bottom: 7px; color: var(--ink2); font-size: 10px; font-family: "JetBrains Mono", ui-monospace, monospace; text-transform: uppercase; letter-spacing: 0.08em; }
+.settings-mini-card b { display: block; color: var(--ink); font-size: 12px; font-weight: 600; overflow-wrap: anywhere; }
+.settings-mini-card.ready { border-color: rgba(114,207,161,0.45); }
+.settings-mini-card.partial { border-color: rgba(220,178,90,0.45); }
+.settings-mini-card.missing { border-color: rgba(220,104,90,0.48); }
+.settings-empty { grid-column: 1 / -1; padding: 12px; border: 1px dashed var(--line-strong); border-radius: 6px; color: var(--ink2); font-size: 12px; }
+.settings-channel-intro { background: rgba(255,255,255,0.018); }
+@media (max-width: 720px) { .settings-modal { width: calc(100vw - 20px); height: calc(100vh - 24px); } .settings-body { flex-direction: column; } .settings-nav { width: auto; max-height: 112px; flex-direction: row; flex-wrap: wrap; border-right: 0; border-bottom: 1px solid var(--line); padding: 8px; } .settings-nav-item { border-radius: 4px; padding: 7px 10px; } .settings-nav-item.active { border-right: 0; box-shadow: inset 0 -2px 0 var(--cool); } .settings-matrix, .settings-capability-grid { grid-template-columns: 1fr; } .settings-hero { flex-direction: column; } }
