diff --git a/src/capabilities/executor.js b/src/capabilities/executor.js
@@
 function getCurrentTurnDeliveryOverride(channelPref, context = {}) {
   const currentExternalId = String(context.currentExternalPartyId || '').trim()
   const currentChannel = context.currentChannel ? normalizeChannel(context.currentChannel) : null
-  if (!currentExternalId || !currentChannel) return null
+  if (!currentChannel) return null
+
+  // Local web/API/voice turns must never leak to a social presence target even if
+  // the model explicitly asks for WECHAT/FEISHU/AUTO. The current visible surface
+  // owns the reply; cross-channel sending needs an explicit user action flow.
+  if (currentChannel === 'TUI') {
+    return { externalTargetId: null, deliveryChannel: 'TUI', isLocal: true }
+  }
+
+  if (!currentExternalId) return null
 
   const pref = (channelPref || 'AUTO').toUpperCase()
   if (pref !== 'AUTO' && currentChannel !== pref) return null
